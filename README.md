# Gatekeeper (Forked from [open-policy-agent/gatekeeper](https://github.com/open-policy-agent/gatekeeper))

**NOTE**: View the `release-X.Y` branches (or associated tags) for relevant releases from `stolostron/gatekeeper`.
